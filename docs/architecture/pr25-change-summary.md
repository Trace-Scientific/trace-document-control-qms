# PR 25 Change Summary

This change restores Railway preview build compatibility while preserving strict compiler checks. It updates TypeScript target/test globals, fixes two application typing defects surfaced by `next build`, and documents the preview-only operational verification path.
